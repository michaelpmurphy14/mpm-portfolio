import React from 'react';

const Resume = () => {
    return (
        <section id="resume" className="p-5">
            <h2>Resume</h2>
            <p>Resume will go here</p>
            {/* You can add a link to download the resume or display it inline */}
        </section>
    );
};

export default Resume;
